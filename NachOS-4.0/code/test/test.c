#include "syscall.h"
#define MAX_LENGTH 32

int main()
{
    // char *buffer;
    // int open = Open("test.txt");
    // Write("hihi", 4, open);
    // Read(buffer, 4, open);
    // PrintString(buffer);
    // PrintString("\n");
    // Remove("test.txt");
    // Close(open);
    Halt();
    return 0;
}